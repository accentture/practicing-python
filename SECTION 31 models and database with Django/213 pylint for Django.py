""" 
    --sqlite was crete after to execute   py manage.py runserver

    -- extention of python allows to clean "linting"  
    --lint of Django display linting for example when work with models and databases
    --when work with Django. We doesn't using queries sql directly, practicing we doesn't use sql; we are using objects

    --to install pylint Django : py -m pip install pylint-django

    -- configurate in setting the next : Python> Linting: Pylint path
 """