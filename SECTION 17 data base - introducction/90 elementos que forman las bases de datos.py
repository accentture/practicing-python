""" 
    ELEMENTOS DE UNA BASE DE DATOS
    *Tablas -> columnas(campos), filas(objetos) y campos
    --la forma de almacenar datos en SQL es mediante una tabla
    --una tabla representa una entidad, también puede representar una asociación de identidades
    *columnas -> son verticales |
    *filas -> horizontales --

    ♥ ejemplo

    --tenemos una base de datos llamada BLOG
    --dentro tiene multiples tablas
        --tabla USUARIOS (o entiddad USUARIO)

 """