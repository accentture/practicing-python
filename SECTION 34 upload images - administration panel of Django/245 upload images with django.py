""" 
    -- if I try upload an image, it will be upload in root of project

    STEPS
        1. create a folder named "media" (named by convention)  in root of project
        2. configurate settings
        3. indicate where will be uploaded image in the model
        4. install Pillow
                py -m pip install Pillow 

        5. upload phot using the admin
        6. if I click image it will be able to be opened, because django can't read system file of project (404 will be displayed)
        7. add a configuration in urls
        
 """