from django.apps import AppConfig


class MiappConfig(AppConfig):
    name = 'miapp' # name of the file
    verbose_name = 'Mi primera aplicacion' # changing name of application in administration panel
